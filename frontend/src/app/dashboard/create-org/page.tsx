import { OrgCreateGuard } from "@/components/dashboard/organization/org-create-guard";

const CreateOrgPage = () => {
  return (
    <OrgCreateGuard>
        Create organization
    </OrgCreateGuard>
  )
}

export default CreateOrgPage;
